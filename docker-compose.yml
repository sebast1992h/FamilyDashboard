services:
  backend:
    build: ./backend
    ports:
      - "4000:4000"
    environment:
      - GOOGLE_API_KEY=your_api_key_here
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend
    restart: unless-stopped
